{
  "name": "itunes-search-app",
  "version": "1.0.0",
  "description": "Application allows the user to search for content within the iTunes Store and Apple Books Store. The user can create a list of ‘favourites’.",
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "start": "node server.js",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix frontend && npm run build --prefix frontend"
  },
  "author": "Joshua Karp",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "helmet": "^3.23.1",
    "node-fetch": "^2.6.1",
    "react-helmet": "^6.1.0",
    "request": "^2.88.2"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^8.0.1"
  },
  "engines": {
    "node": "12.16.0",
    "npm": "6.13.4"
  }
}
